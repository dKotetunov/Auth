<!DOCTYPE html>
<html>
<head>
  <title>Auth</title>
  <link href="/assets/application.css" media="all" rel="stylesheet" type="text/css">
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application", "jquery" %>
  <%= csrf_meta_tags %>
</head>
<body>


<div id="user_nav">
  <% if current_user %>
    <div id="updateUser">Logged in as <%= current_user.email %>.</div>
    <%= link_to "Log out for user", log_out_path %>
  <% elsif current_admin %>
      Logged in as <%= current_admin.email %><br>
      <%= link_to "Log out for admin", admin_log_out_path %>
  <% else %>
    <%= link_to "Sign up for user", sign_up_path %> or
    <%= link_to "log in for user", log_in_path %> or
    <%= link_to "log in for user", admin_log_in_path %>
  <% end %>
</div>

<% flash.each do |name, msg| %>
  <%= content_tag :div, msg, :id => "flash_#{name}" %>
<% end %>


<%= yield %>

</body>
</html>
