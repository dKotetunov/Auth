<h1> Edit admin </h1>
<% if @admin.errors.any? %>
    <div class="errors_messages">
      <h2> <%= pluralize(@profile.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @admin.errors.full_messages.each do |message| %>
            <li> <%= message %> </li>
        <% end %>
      </ul>
    </div>
<% end %>
<%= form_for(@admin, remote: true, url: { action: "update" }) do |f| %>
    <p>
      <%= f.label :username %><br>
      <%= f.text_field :username %>
    </p>
    <p>
      <%= f.label :email %><br>
      <%= f.text_field :email %>
    </p>

    <p>

    </p>
<% end %>